language: java

#env:

jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.m2/repository/

install:
  - git clone https://github.com/cloud-native-java/build.git
  - ./build/install.sh

script:
  - ./build/script.sh